while 1 {
    counter = 3;
    while counter {
        print("stop check");
        stopped = get_velocity(THIS);
        stopped = direc(stopped);
        stopped = not(stopped);
        if stopped {
            veloc = randincircle(96, 1);
            move(THIS, veloc);
        }
        counter = sub(counter, 1);
        wait(500);
    }
    print("flip");
    veloc = randincircle(24, 1);
    move(THIS, veloc);
}